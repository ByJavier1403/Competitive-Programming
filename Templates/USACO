#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
  freopen("roboherd.in", "r", stdin);
  freopen("roboherd.out", "w", stdout);
  //Leer con ri(variable); e imprimir con cout
}
