#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solved(){

}

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  cout<<fixed;
  int n;
  cin>>n;
  while(n--){
    solved();
  }
}
